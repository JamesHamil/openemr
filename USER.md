# AgentForge User Document

The full target-user analysis lives in [`USERS.md`](USERS.md).

This compatibility file exists because the AgentForge PDF references both `USER.md` and `USERS.md` in different places. The source of truth is `USERS.md`, which defines:

- the target user: a hospitalist preparing for inpatient rounds;
- the workflow moment: rapid patient review before entering the room;
- the specific use cases the Clinical Co-Pilot will support;
- why a conversational agent is the right solution for each use case.
